#MVP системи моніторингу законодавчих змін України для агрохолдингу:

- забирає з порталу ВРУ список зареєстрованих за період законопроєктів (усі сторінки пагінації),
- парсить номер, дату реєстрації, людську назву законопроєкту (law_title) і посилання на картку,
- зберігає версії вмісту картки (з diff від попередньої версії),
- надає просте REST API,
- є легкий React-UI для перегляду (список, фільтри, деталі, версії з підсвіткою diff).

##Налаштування середовища
```bash
npm i
npm run dev
```

##Збірка та запуск бекенду
```bash
npm i
npm run build
npm run run  //запустити програму, яка збирає дані про закони і зберігає їх в базу даних
npm run api  //запустити api
```
